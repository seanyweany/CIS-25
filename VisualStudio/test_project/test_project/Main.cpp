#include "Main.h"

// using namespace std;

int main() {
	int input, input2;

	std::cout << input << std::endl;

	/*
	std::cout << "Please enter a number: ";
	std::cin >> input;

	std::cout << "Please enter another number: ";
	std::cin >> input2;
	
	std::cout << input << " + " << input2 << " = " << input + input2 << "." << std::endl;

	std::cout << "\nmemory (malloc):\n" << std::endl;

	int* inputs = (int*)malloc(2 * sizeof(int));
	inputs[0] = input;
	inputs[1] = input2;

	for (int i = 0; i < sizeof(inputs); i++) {
		std::cout << &inputs[i] << std::endl;
	}

	free(inputs);
	*/

	return 0;
}